module MaterialsHelper
#	def list_transcriptions(list)
#		return "" if list == nil || list.length == 0
#		lines = []
#		list.each_with_index {|rec, i|
#			lines.push(link_to(rec.title, rec.pdf.url))
#		}
#		return lines.join("<br />")
#	end
end
